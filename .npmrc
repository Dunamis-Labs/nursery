# Skip optional dependencies during install to speed up builds
# Puppeteer and Playwright are only needed for data import scripts, not web builds
# They're in optionalDependencies so they won't block the build if unavailable
legacy-peer-deps=false

